# calibrax.profiling.hardware

Hardware detection and platform-specific specs for JAX backends.
Provides reference peak FLOP/s and memory bandwidth values for
common accelerators (TPU v5e, A100, H100, CPU) and utilities
for auto-detecting the active JAX backend.

::: calibrax.profiling.hardware
    options:
      show_root_heading: false
